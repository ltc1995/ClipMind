# ClipMind 产品需求文档(PRD)

- 文档版本:v1.0(初稿)
- 编写日期:2026-08-03
- 产品名称:ClipMind 智能剪贴板助手
- 当前代码版本:1.0.2(对应 `utils/config.py` 中 `APP_VERSION`)

---

## 1. 产品定位

### 1.1 一句话介绍

ClipMind 是一款 Windows 平台上的 AI 增强型智能剪贴板管理工具,通过自动记录用户的复制历史,并提供搜索、分类、收藏、标签能力,把系统剪贴板变成用户个人的知识库。

### 1.2 背景与痛点

Windows 用户在日常工作(尤其编程、测试、资料收集)中反复复制粘贴,面临以下问题:

| 痛点 | 说明 |
|------|------|
| 复制内容易丢失 | 新的复制会覆盖旧的剪贴板内容,重要片段无法找回 |
| 系统自带剪贴板历史弱 | Windows 自带历史(Win+V)功能有限,重启失效、无分类、无搜索 |
| 长文本/代码/资料难以管理 | 复制过的代码、日志、Bug 信息、网页资料没有统一存放处 |
| 查找困难 | 内容一多就找不到,需要按内容、标签快速定位 |
| 缺少个人知识沉淀 | 碎片信息复制后即遗忘,无法积累成个人知识库 |

### 1.3 产品目标

1. **零成本记录**:用户无需任何操作,复制即自动入库。
2. **即搜即用**:任意场景(浏览器、IDE、聊天软件)按下全局快捷键即可搜索历史并一键恢复复制。
3. **本地优先、隐私安全**:所有数据默认仅存本地,不上传、不依赖云端。
4. **轻量常驻**:后台运行占用低,不影响日常使用。

### 1.4 非目标(MVP 不做)

- 不依赖任何 AI 接口/外部服务,首版完全离线可用。
- 不做云同步、多设备同步。
- 不做账号体系、付费订阅(版本区分仅用于内部标识)。
- 不做系统级剪贴板接管(不拦截、不篡改复制内容,只做监听与记录)。

---

## 2. 目标用户

### 2.1 用户画像

| 用户群 | 典型场景 | 核心诉求 |
|--------|----------|----------|
| 程序员 | 复制代码片段、报错日志、命令行、接口文档 | 快速找回代码、按语言识别分类、收藏常用片段 |
| 测试工程师 | 保存 Bug 描述、SQL、接口请求参数、测试数据 | 跨用例复用测试数据、按关键字检索历史 |
| 普通办公用户 | 收集网页资料、聊天内容、临时文本 | 内容不丢、能搜索、界面简单 |

### 2.2 用户场景示例

- 场景 A(程序员):写代码时复制了一段报错日志,两小时后忘了出处,按 `Ctrl+Shift+V` 输入关键字秒搜到并重新复制。
- 场景 B(测试):整理测试用例时需要一条两周前复制过的 SQL,打开主窗口在过滤栏选择"代码"或直接搜索即可找回。
- 场景 C(办公):搜集资料时截图并存档,ClipMind 自动保存图片及缩略图,右键可"另存为"。

### 2.3 目标平台

- Windows 10 / Windows 11(x64),打包为独立 `ClipMind.exe`。

---

## 3. 核心功能清单

> 状态说明:`[已实现]` = 当前代码已可用;`[规划]` = 列入后续版本,本期不做。

### F1 剪贴板自动监听与保存(已实现)

- 监听 Windows 系统剪贴板,用户复制(Ctrl+C / 剪切 / 截图等改变剪贴板的行为)后自动保存。
- 监听机制:Qt `dataChanged` 信号触发即时检查 + 0.5 秒定时轮询兜底,确保不遗漏。
- 支持内容类型:
  - **文本**:保存完整内容,生成预览(单行化,超 100 字截断)。
  - **代码**:通过内置关键词表自动识别语言(Python / JavaScript / TypeScript / Java / Go / Rust / C++ / C# / SQL / Shell / YAML / JSON / HTML / CSS 共 14 种),记录 `content_type=code` 与 `language`。
  - **图片**:自动保存为 PNG 文件,记录图片路径、缩略图,以像素指纹做去重。
- 去重规则:
  - 内容完全相同(含图片指纹相同)的记录不重复入库,仅更新时间戳,使常用内容"浮到最前"。
  - 用户主动复制 ClipMind 回写的剪贴板内容时不重复记录(防回环)。
  - 已删除的记录在用户重新复制前不会自动重新入库。

### F2 历史记录列表(已实现)

- 主窗口展示剪贴板历史,每条记录显示:类型图标、内容预览、创建时间(相对时间,如"3 分钟前/昨天")、标签徽章、语言徽章、收藏标记。
- 顶部过滤栏支持按类型筛选:**全部 / 收藏 / 代码 / 文本 / 图片**。
- 顶部状态栏显示历史总量与当前展示条数。
- 列表随窗口激活自动刷新。

### F3 搜索(已实现)

- 主窗口搜索框 + 快捷搜索弹窗共用同一搜索能力:按关键字对**内容**与**标签**做模糊匹配(`LIKE`),按时间倒序返回,最多 50 条。
- 数据库层已建立 SQLite FTS5 全文索引表(`clipboard_fts`)与同步触发器,为后续升级全文检索预留能力。

### F4 快捷呼出(已实现)

- 全局快捷键 `Ctrl + Shift + V`(经注册表级全局热键实现,支持 `Ctrl/Shift/Alt` 组合)。
- 呼出逻辑:主窗口未显示/最小化时恢复主窗口;主窗口可见时切换显示无边框置顶的"快速搜索"弹窗。
- 快速搜索弹窗:置顶、无边框、输入即搜,`Esc` 关闭,点击/回车选中即复制。
- 系统托盘常驻:左键单击托盘图标显示/隐藏主窗口,托盘右键菜单提供"显示 ClipMind / 快速搜索 / 设置 / 退出"。

### F5 复制恢复(已实现)

- 点击历史记录(单击/双击、弹窗回车)即把内容写回系统剪贴板,用户直接 `Ctrl+V` 粘贴。
- 文本与代码恢复原文;图片恢复为剪贴板图片对象。
- 恢复复制后界面给出成功反馈,并刷新排序。

### F6 标签管理(已实现)

- 右键记录 →"编辑标签",以逗号分隔输入多个标签(如 `#python, #学习`)。
- 标签在列表中以色块徽章展示,并参与搜索匹配。

### F7 收藏(已实现)

- 右键记录 →"收藏/取消收藏",收藏项显示 ⭐ 标记,可通过"收藏"过滤栏单独查看。

### F8 删除与数据管理(已实现)

- 右键单条删除(二次确认)。
- 设置弹窗提供"清空所有历史记录"(二次确认,不可恢复)。
- 单条删除后内容进入"已删除"集合,防止监听误回写。

### F9 设置与开机启动(已实现)

- 设置弹窗:开机自动启动(写入 HKCU Run 注册表)、查看全局快捷键、数据存储说明、清空历史、版本与关于信息。
- 首次启动显示欢迎引导页,之后不再弹出。

### F10 单实例与打包(已实现)

- 通过临时目录锁文件保证同一时间仅一个 ClipMind 实例运行。
- `build.py` 基于 PyInstaller 一键打包为 `ClipMind.exe`(onefile、无控制台窗口、内置图标与资源)。

### F11 后续 AI 增强(规划,V2)

| 功能 | 说明 |
|------|------|
| AI 自动总结 | 长文章自动生成摘要与标签 |
| AI 智能分类 | 复制内容后自动分析归类 |
| 智能搜索 | 自然语言语义搜索(规划引入向量数据库) |
| OCR 识别 | 截图自动识别文字并归类 |

---

## 4. 验收标准

> 以下标准均基于当前已实现功能(代码版本 1.0.2)。验收方式:在 Windows 10/11 上运行打包产物 `ClipMind.exe` 或源码 `python main.py` 逐条验证。

### 4.1 功能验收

| 编号 | 验收项 | 通过标准 |
|------|--------|----------|
| A1 | 自动保存文本 | 复制一段文本后,主窗口列表出现该记录,预览、时间正确 |
| A2 | 自动识别代码 | 复制 `def hello():` 等 Python 代码,记录类型显示"代码"且语言徽章为 python |
| A3 | 自动保存图片 | 复制截图后,列表出现图片缩略图,`data/images/` 下生成 PNG 文件 |
| A4 | 去重 | 连续复制相同内容,列表不产生重复记录,该记录时间戳更新并置顶 |
| A5 | 过滤 | 分别点击"全部/收藏/代码/文本/图片",列表只显示对应类型 |
| A6 | 搜索 | 输入关键字能命中内容或标签含该词的记录,无关键字时显示最近 50 条 |
| A7 | 快捷呼出 | 在其他软件(如浏览器)前台时按 `Ctrl+Shift+V`,弹出 ClipMind 主窗口或搜索弹窗 |
| A8 | 复制恢复 | 点击历史记录后,在任意输入框 `Ctrl+V` 能粘贴出原内容(文本/代码/图片均验证) |
| A9 | 收藏 | 收藏后记录显示 ⭐,切换"收藏"过滤可见;取消收藏后消失 |
| A10 | 标签 | 编辑标签保存后徽章正确显示,且搜索标签关键字能命中 |
| A11 | 删除 | 删除单条记录后列表消失,且重新复制该内容不会自动回库(再次主动复制除外) |
| A12 | 清空历史 | 设置中"清空所有历史记录"确认后列表为空,库中计数为 0 |
| A13 | 开机启动 | 勾选开机自启后,注册表 `HKCU\...\Run` 出现 ClipMind 项;取消后移除 |
| A14 | 托盘 | 关闭主窗口后程序仍在托盘,左键/右键菜单功能正常,可退出 |
| A15 | 单实例 | 程序已运行时再次启动,提示"已在运行"并退出,不产生第二个实例 |
| A16 | 打包 | `python build.py` 成功产出 `dist/ClipMind.exe`,双击可运行且无控制台窗口 |

### 4.2 非功能验收

| 编号 | 类别 | 验收项 | 通过标准 |
|------|------|--------|----------|
| B1 | 性能 | 后台常驻资源占用 | 空闲状态 CPU 占用 < 1%,内存占用 < 100MB |
| B2 | 性能 | 启动速度 | 双击启动到窗口可交互 < 3 秒 |
| B3 | 性能 | 搜索响应 | 50 条以内结果列表检索响应 < 0.5 秒,输入即时刷新 |
| B4 | 隐私安全 | 数据本地化 | 所有历史数据仅存本地 SQLite(`data/clipmind.db`)与图片目录,代码中无任何上传逻辑 |
| B5 | 隐私安全 | 用户可控删除 | 支持单条删除与一键清空,删除后数据库中对应记录不存在 |
| B6 | 健壮性 | 异常隔离 | 剪贴板读取/图片保存失败时程序不崩溃,后台打印错误并继续监听 |
| B7 | 兼容性 | 系统支持 | Windows 10 与 Windows 11 均可安装运行 |
| B8 | 稳定性 | 长时间运行 | 连续运行 8 小时无崩溃、无内存显著增长(泄漏),历史记录持续正常入库 |

### 4.3 验收结论口径

- 功能验收 A1–A16 全部通过,且非功能验收 B1–B8 达标,视为本版本验收通过。
- 任一 A 类项失败即为阻塞性缺陷,须修复后回归;单一 B 类项不达标记录为优化项,进入下一迭代处理。

---

## 附:文档与实现差异说明(供后续维护参考)

| 项目 | README 描述 | 当前实现现状 |
|------|-------------|--------------|
| 开发阶段 | Phase 1 基础版本 | 已进入 Phase 2(托盘、设置、开机启动、图标、快捷弹窗均已实现) |
| 搜索 | SQLite FTS 全文搜索 | 当前实际使用 `LIKE` 模糊匹配;FTS5 表与触发器已建好但尚未作为查询路径启用 |
| 图片 | 支持类型中未明确 | 已完整支持图片监听、存盘、缩略图、复制恢复、另存为 |
| 内容去重 | 未明确 | 已实现内容唯一索引 + 时间戳置顶 + 防回环机制 |

> 本 PRD 以**当前实际代码行为**为准编写;README 为历史规划文档,建议后续按 PRD 同步更新。
